import styled from "styled-components";

const ImageSection = styled.section`
  position: relative;
  width: inheret;
  margin: 20px;
  display: flex;
  flex-direction: column;
`;

const ImageContainer = styled.img`
  padding-bottom: 10px;
`;

export { ImageSection, ImageContainer };
